This post demonstrates the power of Shiki for syntax highlighting in MDX. Shiki uses the same syntax highlighting engine as VS Code, providing beautiful and accurate code highlighting.

## JavaScript Example

Here's a JavaScript function with proper syntax highlighting:

```javascript
function fibonacci(n) {
  if (n <= 1) return n;
  return fibonacci(n - 1) + fibonacci(n - 2);
}

const result = fibonacci(10);
console.log(`Fibonacci(10) = ${result}`);
```

## TypeScript with React

Here's a React component written in TypeScript:

```typescript
import React, { useState } from 'react';

interface CounterProps {
  initialValue?: number;
}

export const Counter: React.FC<CounterProps> = ({ initialValue = 0 }) => {
  const [count, setCount] = useState(initialValue);

  return (
    <div className="counter">
      <p>Count: {count}</p>
      <button onClick={() => setCount(count + 1)}>Increment</button>
      <button onClick={() => setCount(count - 1)}>Decrement</button>
    </div>
  );
};
```

## Python Example

Python code also looks beautiful:

```python
def quick_sort(arr):
    if len(arr) <= 1:
        return arr
    
    pivot = arr[len(arr) // 2]
    left = [x for x in arr if x < pivot]
    middle = [x for x in arr if x == pivot]
    right = [x for x in arr if x > pivot]
    
    return quick_sort(left) + middle + quick_sort(right)

numbers = [3, 6, 8, 10, 1, 2, 1]
print(quick_sort(numbers))
```

## CSS Styling

Even CSS is beautifully highlighted:

```css
.container {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  min-height: 100vh;
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
}

.card {
  padding: 2rem;
  border-radius: 1rem;
  box-shadow: 0 20px 25px -5px rgba(0, 0, 0, 0.1);
  background: white;
  transition: transform 0.3s ease;
}

.card:hover {
  transform: translateY(-5px);
}
```

## JSON Configuration

JSON files are also supported:

```json
{
  "name": "maxubrqcool",
  "version": "1.0.0",
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start"
  },
  "dependencies": {
    "next": "^14.0.4",
    "react": "^18.2.0",
    "shiki": "^1.0.0"
  }
}
```

## Inline Code

You can also use inline code like `const x = 10` or `npm install shiki` within your text, and it will be styled appropriately.

## Multiple Languages

Shiki supports hundreds of languages including:

- **JavaScript/TypeScript** - For web development
- **Python** - For data science and scripting
- **Rust** - For systems programming
- **Go** - For backend services
- **SQL** - For database queries
- And many more!

## Conclusion

With Shiki integrated into your MDX blog, your code examples will look professional and be easy to read. The VS Code-quality syntax highlighting makes technical content much more accessible and visually appealing.

